{
  "name": "spike-regen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Spike §18.2 — idempotent partial generation/regeneration with codegen/human ownership and explicit conflicts. Disposable.",
  "scripts": {
    "demo": "tsx src/demo.ts",
    "test": "node --import tsx --test test/regen.test.ts",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.0"
  }
}
